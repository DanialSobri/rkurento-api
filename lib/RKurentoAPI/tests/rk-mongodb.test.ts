import Room from '../rk-room';
import { expect } from 'chai';

describe('RKurento MongoDB Test', function() {
    it('Connect with Database', function() {
        // let room = new Room();
        // expect(room.updateRoomdb()).equal(true);
    });
    it('Initialize Database', function() {
        // let room = new Room();
        // expect(room.updateRoomdb()).equal(true);
    });
    it('Write on Database', function() {
        // let room = new Room();
        // expect(room.updateRoomdb()).equal(true);
    });
    it('Read from Database', function() {
        // let room = new Room();
        // expect(room.updateRoomdb()).equal(true);
    });
});