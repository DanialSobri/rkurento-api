import { expect } from 'chai';
import { getSessions
         } from '../rkurento-api';

describe('RKurento API Test', function() {
    it('Get active Session', function() {
        // let room = new Room();
        // expect(room.updateRoomdb()).equal(true);
    });
});